#this monitor checks the .git files:
#(1) 

import sys
import os

class monitor:

    """monitor the git files change"""

    def __init__(self, repo_path = '/'):
        self.git_path = repo_path + "/.git/"
        self.
        
    def check_file(self):
        
        if os.path.exists()
